[package]
name = "obs-websockets-replay-buffer-save"
version = "1.0.1"
edition = "2024"

[dependencies]
serde_json = "1.0"
chrono = "0.4.41"
obws = "0.14.0"
tokio = { version = "1.47.1", features = ["rt-multi-thread", "macros"] }
